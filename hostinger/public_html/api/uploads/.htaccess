# Los adjuntos NO se sirven directamente: solo a través de /api/index.php/archivos/ver
# (que verifica la sesión). Bloquea el acceso web directo a esta carpeta.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
